alert("Webpack and React working");

//TESTING BABEL - using const, export, template, arrow f, 
const greetings = (person, text) => {
  return `${person} ${text}. This is a templae literal example with ES6`;
}

export default greetings;